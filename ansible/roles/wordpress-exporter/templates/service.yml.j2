apiVersion: v1
kind: Service
metadata:
  name: wordpress-exporter
  namespace: wordpress
spec:
  selector:
    app: wordpress-exporter
  ports:
    - port: 9100
      targetPort: 9100
  type: LoadBalancer

